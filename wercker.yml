box: golang
dev:
  steps:
    - internal/watch:
        code: |
          go get -u github.com/appscode/chartify
        reload: true
# Build definition
build:
  # The steps that will be executed on build
  steps:
    # golint step!
    - wercker/golint
    - script:
        name: debug
        code: |
          ls -l
    # Build the project
    - script:
        name: go build
        code: |
          go get -u github.com/appscode/chartify
